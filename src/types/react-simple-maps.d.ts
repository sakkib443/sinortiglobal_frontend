// react-simple-maps@3 ships no TypeScript types and the project runs React 19.
// The library is runtime-compatible; this ambient declaration lets the
// production type-check pass (imports resolve as `any`).
declare module 'react-simple-maps';
